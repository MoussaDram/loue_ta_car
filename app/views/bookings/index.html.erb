<div class="container py-5">
  <div class="row">
    <div class="col-12 ">
      <div class="bg-white rounded p-3 shadow">
        <h1>Toutes vos réseravations</h1>
        <% @bookings.each do |booking| %>
          <h3><%= booking.car.brand %></h3>
          <p><%= booking.start_at %></p>
          <p><%= booking.end_at %></p>


        <% end %>
      </div>
    </div>
  </div>
</div>
